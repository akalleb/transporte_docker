# Configuração do Supabase (Obrigatório)
SUPABASE_URL=https://sua-url-supabase.supabase.co
SUPABASE_KEY=sua-chave-anonima
SUPABASE_SERVICE_ROLE_KEY=sua-chave-service-role

# Configuração de Portas (Docker)
# Se as portas 3000/3001 estiverem ocupadas no VPS, altere aqui (ex: 8080 e 8081)
APP_PORT=8080
WHATSAPP_PORT=8081

# Configuração do Servidor WhatsApp
PORT=3001
# IMPORTANTE: Se alterar APP_PORT acima, atualize o CORS_ORIGIN com a nova porta
CORS_ORIGIN=http://seu-ip-vps:8080
# Exemplo produção: http://seu-dominio.com:8080

# URL da API do WhatsApp para o Frontend Nuxt
# Deve ser acessível pelo navegador do usuário
# IMPORTANTE: Se alterar WHATSAPP_PORT acima, atualize esta URL com a nova porta
WHATSAPP_API_URL=http://seu-ip-vps:8081
# Exemplo produção: http://seu-dominio.com:8081

# Configuração do Banco de Dados PostgreSQL (Opcional - para migração futura)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=sua_senha_secreta
POSTGRES_DB=app_db
